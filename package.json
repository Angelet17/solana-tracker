{
  "name": "solana-tracker",
  "version": "1.0.0",
  "description": "Webhook para rastrear transacciones de SOL desde KuCoin",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "vercel-build": "npm install && echo 'Build completo'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Tu Nombre",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.9.0"
  },
  "engines": {
    "node": ">=16.x"
  }
}
